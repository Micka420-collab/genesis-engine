# Genesis Engine — État du projet

**Dernière mise à jour :** 18 juin 2026 (J+8 — **Cap. C12 `forced_draught`** : 12ᵉ capacité agent et le **2ᵉ APPARATUS** (le pendant de C11) — le **tirage forcé** (soufflet + charbon de bois) : la **VOÛTE** que C9 (`vitrifies_if_kiln_fired`) ET C11 (`vitrifies_if_forced_draught`) désignent *toutes deux*. Un four à tirage *naturel* (C11) plafonne (~1000–1150 °C) sous la vitrification et la métallurgie ; **souffler** de l'air sur du **charbon** enclos pousse la pointe dans le régime du bas-fourneau (~1100–1400 °C). Ce n'est PAS une transformation de matière mais un **apparatus** : il **élève encore la pointe** que voit la matière. **SSOT** `forced_draught_peak_c(fine_fuel, wall_refractory)` = pointe du four naturel (**réemploi C11** `kiln_peak_temp_c`, *le combo*) + gain du tirage forcé **plafonné par la réfractarité de paroi** (commune `shale` 1100 °C / réfractaire kaolin `fine_clay` 1400 °C ; veille archéométrie bas-fourneau : soufflet ~1100–1300 °C). Il **RÉALISE** enfin la vitrification (`vitrifies_watertight` **True** pour le kaolin réfractaire — l'arc « mensonge du kaolin C9 → paroi C11 → vitrification C12 » se ferme : open-fire firedness 0,64 → four 0,86 → **forcé 1,00**) et **OUVRE** la métallurgie : `reaches_copper_smelting_temp` (≥1085 °C, fusion du cuivre) × `copper_ore_here` (compose le tell vert C1) → `would_smelt_copper_here` ; la **fonte effective** (consommer le minerai → métal) reste différée (Cap. C13), comme `reaches_iron_bloomery_temp` (1200 °C, paroi réfractaire). **N'introduit AUCUN nouveau tell** (pas de `_PROFILE`, `PY_TO_RUST` inchangé à 15, hors glob `*_outcrop.py`) : garde-fou D8 **par composition** (6ᵉ démonstration après C7/C8/C9/C10/C11), `test_introduces_no_new_tell`. Invariant « le monde ne ment jamais » prouvé sur monde Genesis réel (seed `0xBEEF`, 144/144 forçables = 127 communes + 17 réfractaires qui **vitrifient**, 144 atteignent le cuivre, 17 le fer, 0 violation) + `forced_draught_preview` non mutant. 21 tests + smoke `p144` 7/7, pytest **634/634**, global ~80,2 % (transformation/actionnabilité). Antérieur J+7 run #2 — **Cap. C11 `kiln_draft`** : 11ᵉ capacité agent et l'**APPARATUS du four à tirage** — la **VOÛTE** que C9 (`vitrifies_if_kiln_fired`) ET C10 (`would_mortar_if_kiln_fired`) désignent *toutes deux* : enfermer un feu (C7) dans une enceinte d'argile (C5) et lui donner un tirage l'élève de ~850 °C (feu nu) à ~1000–1100 °C (four), assez pour cuire le calcaire pur **à cœur** (mortier liant, C10 **réalisé** — 87/144 sites) et fritter le kaolin en **corps sain** (C9 **racheté**). Ce n'est PAS une transformation de matière mais un **apparatus** (le pendant de C7) : il **élève la pointe de température** que voit la matière. **SSOT** `kiln_peak_temp_c(fine_fuel, wall_refractory)` = pointe du feu nu (**réemploi C9** `open_fire_peak_temp_c`, *le combo*) + gain d'enceinte **plafonné par la réfractarité de paroi** (commune `shale` 1000 °C / réfractaire kaolin `fine_clay` 1150 °C ; veille pyrotechnologie : four à tirage ~1000–1100 °C, fire-clay 1515–1775 °C). **L'inversion DE l'inversion** : le kaolin réfractaire — la *mauvaise* argile de poterie de C9 (sous-cuite au feu ouvert, firedness 0,64) — est la **meilleure argile de PAROI** ; c'est *lui* qui bâtit le four assez chaud (1070 °C) pour, enfin, cuire le kaolin **sain** (firedness 0,86). `vitrifies_watertight` **toujours False** en tirage naturel → `vitrifies_if_forced_draught` diffère la vitrification complète + la métallurgie au **soufflet + charbon** (1100–1300 °C, bas-fourneau ; C12+). **N'introduit AUCUN nouveau tell** (pas de `_PROFILE`, `PY_TO_RUST` inchangé à 15, hors glob `*_outcrop.py`) : garde-fou D8 **par composition** (5ᵉ démonstration après C7/C8/C9/C10), `test_introduces_no_new_tell`. Invariant « le monde ne ment jamais » prouvé sur monde Genesis réel (seed `0xBEEF`, 144/144 constructibles = 127 parois communes + 17 réfractaires, 87 réalisent le mortier liant, 0 violation) + `kiln_preview` non mutant. 23 tests + smoke `p143` 7/7, pytest **613/613**, global ~80,1 % (transformation/actionnabilité). Antérieur J+7 (run #1) — **Cap. C10 `lime_burning`** : 10ᵉ capacité agent et **troisième capacité de TRANSFORMATION** — la **cuisson de la chaux** : brûler un calcaire (C6) dans un feu (C7) crée la **chaux** (CaCO₃ → CaO + CO₂), le plus ancien liant chimique connu (enduits néolithiques de Göbekli Tepe ~9500 av. J.-C., antérieur à la métallurgie) — **pendant exact de C9** (l'argile *contient*, le calcaire *lie*). **Compose** C6 `limestone_outcrop` (calcaire + `lime_grade` + `lime_class` + `mortar_grade`) × C7 `fire_ignition` (feu + `fine_fuel`) et **réutilise verbatim** la SSOT de température de C9 `open_fire_peak_temp_c` (**le combo de la veille** : un seul feu, deux pyrotransformations) — effet 1+1>2 : calcination possible QUE si calcaire ET feu coexistent. Physique **calculée** (veille thermochimie de la chaux, Boynton) : seuil de décarbonatation — calcaire commun/dolomitique ~680 °C → **bien cuit** au feu ouvert (`lime_yield` 0,72) ; **calcaire pur réfractaire ~770 °C → sous-cuit** (`lime_yield` 0,12), conversion complète ~898 °C : *le mensonge rendu visible* — la meilleure pierre (`mortar_grade`, peut lier le mortier) **déçoit au feu nu** car il faudrait un **four à chaux** ≥900 °C (pendant du kaolin C9 / de l'obsidienne C8). `mortar_ready` **toujours False** en feu ouvert (→ capacité « four à chaux » future). **N'introduit AUCUN nouveau tell** (pas de `_PROFILE`, `PY_TO_RUST` inchangé à 15, hors glob `*_outcrop.py`) : garde-fou D8 **par composition** (4ᵉ démonstration après C7/C8/C9), asservi par `test_introduces_no_new_tell`. Invariant « le monde ne ment jamais » prouvé sur monde Genesis réel (seed `0xBEEF`, 131/144 chunks cuisibles = 44 bien cuits + 87 sous-cuits, 0 violation) + boucle calcaire+foyer cuit / calcaire pur vu idéal mais sous-cuit (`burn_preview` non mutant). 20 tests + smoke `p142` 7/7, pytest **590/590**, global ~80,0 % (transformation/actionnabilité). Antérieur J+6 run #2 — **Cap. C9 `ceramic_firing`** : 9ᵉ capacité agent et **deuxième capacité de TRANSFORMATION** — la **cuisson de la céramique** : cuire une argile (C5) dans un feu (C7) crée un matériau qui n'existe pas dans la nature (la transformation néolithique fondatrice — le récipient qui *contient* l'eau C3, le grain, le métal). **Compose** C5 `clay_outcrop` (argile + `pottery_grade` + `ceramic_grade`) × C7 `fire_ignition` (feu + `fine_fuel`) — effet 1+1>2 : cuisson possible QUE si argile ET feu coexistent. Physique **calculée** (veille archéométrie 2025) : pointe du feu ouvert 600–850 °C (selon combustible) confrontée à la maturation de l'argile — terre commune `shale` ~700 °C → **saine** (`ware` 0,45) ; **kaolin `fine_clay` réfractaire ~1250 °C → sous-cuit** (`ware` 0,16) : *le mensonge rendu visible* — la meilleure argile (pottery_grade 0,85, peut vitrifier étanche) **déçoit au feu nu** car il faudrait un four ≥1100 °C (pendant symétrique de l'obsidienne C8). `watertight` **toujours False** en feu ouvert (pas de vitrification → capacité « four » future). **N'introduit AUCUN nouveau tell** (pas de `_PROFILE`, `PY_TO_RUST` inchangé à 15, hors glob `*_outcrop.py`) : garde-fou D8 **par composition** (3ᵉ démonstration après C7/C8), asservi par `test_introduces_no_new_tell`. Invariant « le monde ne ment jamais » prouvé sur monde Genesis réel (seed `0xBEEF`, 144/144 chunks cuisibles = 127 terre saine + 17 kaolin sous-cuit, 0 violation) + boucle argile+foyer cuit / kaolin vu idéal mais sous-cuit (`firing_preview` non mutant). 18 tests + smoke `p141` 7/7, pytest **570/570**, global ~80,0 % (transformation/actionnabilité). Antérieur même jour (run #1) — **Cap. C8 `lithic_tempering`** : 8ᵉ capacité agent et **première capacité de TRANSFORMATION** (pas perception) — la **trempe thermique de la pierre** : chauffer un silex/chert dans un foyer *améliore sa taille* (`base_quality`→`tempered_quality`), la plus ancienne pyrotechnologie connue après le feu lui-même, antérieure à la poterie et à la métallurgie (silcrète chauffé à Pinnacle Point ~72 ka). **Compose** C2 `lithic_outcrop` (pierre + `knap_quality` incl. silex/chert) × C7 `fire_ignition` (feu faisable) — effet 1+1>2 : trempe possible QUE si silice réactive ET feu coexistent. Quatre réponses honnêtes : chert **+0,20** / quartzite +0,12 / **obsidienne 0** (déjà du verre — *le mensonge rendu visible* : la meilleure pierre à tailler, mais le feu ne l'améliore pas) / non-silice 0. **N'introduit AUCUN nouveau tell** (pas de `_PROFILE`, `PY_TO_RUST` inchangé à 15 entrées, fichier hors glob `*_outcrop.py`) : garde-fou D8 respecté **par composition** (2ᵉ démonstration après C7), asservi par `test_introduces_no_new_tell`. Invariant « le monde ne ment jamais » prouvé sur monde Genesis réel (seed `0xBEEF`, 84/144 chunks temperables = 76 chert + 8 quartzite, 0 violation) + boucle silex+foyer se trempe / obsidienne vue idéale mais sans gain (`temper_preview` non mutant). **Aussi livré** : `native/world-engine/crates/STATUS.md` (ferme **R1** BLIND-SPOTS, 23 crates classées : 20 active / 2 entrypoints / `gpu` dormant / `geology` orphelin=ADR-0007). 16 tests + smoke `p140` 7/7, pytest **552/552**, global ~80,0 % (transformation/actionnabilité, pas nouveau point réalisme). Antérieur J+5 *suite* — **Cap. C7 `fire_ignition`** : 7ᵉ capacité agent, l'**amorçage du feu** — la **voûte qui ferme l'arc C1→C6** : presque toute matière rendue perceptible demande *un feu* pour devenir outil (fondre le cuivre C1, brûler le combustible C4, cuire l'argile C5, calciner le calcaire C6) ; sans amorçage *par l'agent*, ces capacités restaient des matières inertes. `engine.wildfire` (Wave 14) modélise le feu **spontané** (foudre + propagation) et note que l'agent doit *déduire* que « le silex frappé produit la même chose en petit », mais aucun signal de substrat ne disait, par site, *si* un humain peut allumer un feu ici et *comment*. C7 expose l'affordance par **deux voies physiques honnêtes** (veille du jour confirmée) : **PERCUSSION** — pyrite (FeS₂ pyrophorique) frappée par un percuteur dur (silex) sur amadou ≤ 0,58 (la **plus ancienne production de feu connue** : site néandertalien ~400 ka, méthode d'Ötzi ~3300 av. J.-C.) ; **FRICTION** — sans minéral, amadou *très* sec ≤ 0,45 (archet/drille). Effet 1+1>2 : géologie (pyrite+silex, SYSTÈME C) × hydrologie (`chunk.water`, SYSTÈME A) × biome combustible (SYSTÈME E). **N'introduit AUCUN nouveau tell minéral** (pas de `_PROFILE`, pas d'entrée `PY_TO_RUST`) : il **compose** des tells déjà classés cross-langage — la pyrite (gossan C1) + le percuteur (pétrologie C2 réutilisée *verbatim*) — décision asservie par `test_introduces_no_new_tell` (garde-fou D8 respecté par composition, pas contourné). Distinct de `wildfire` (feu spontané) — **complémentaire**. Sociétés/agents 77→78, global **~80,0 %** (cible atteinte), pytest **536/536**, smoke `p139` 7/7, seed `0xBEEF` (prairie). Antérieur J+5 — **ADR-0008** frontière Python/Rust *(la couche de simulation active est `runtime/engine` côté Python pour l'ère cargo-less ; Rust gelé Wave 42, réactivation conditionnelle)* + **garde-fou D8** rendant le moratoire de tells une porte CI ; pytest 516/516. Antérieur J+4 — **Cap. C6 `limestone_outcrop`** : 6ᵉ capacité agent, découverte émergente du **calcaire** — le **pendant construction** de l'argile (C5 *contient* : récipient/four/creuset/brique ; C6 *bâtit & colle* : pierre de taille + chaux/mortier). La veille ancre le maillon dans l'âge de pierre : la **chaux est le plus ancien liant connu** (néolithique ~9500 av. J.-C., antérieur à la métallurgie). Le calcaire restait muet ET le crate Rust réservait un `Mineral::LimestonePure` **orphelin** depuis Wave 43. C6 le **ferme** : nouveau minéral catalogue `limestone_pure` (CaCO3 pur), **deux propriétés honnêtes orthogonales** — D1 grade de chaux (pureté → `mortar_grade`, seul le pur fait le mortier) × D3 porte d'altération (`sound_quarry` dressable / `karst_fissured` humide / `frost_shattered` gel — lien Wave 50), tell calcaire pur `(245,240,225)` byte-exact ⇔ `Mineral::LimestonePure` (4ᵉ couleur verrouillée), `PY_TO_RUST` enrichi. Géologie 77→78, global ~79,9 %, pytest 513/513, smoke `p138` 7/7, seed `0xC1A7`. Antérieur — **Cap. C5 `clay_outcrop`** : 5ᵉ capacité agent, découverte émergente de l'**argile** — la **clé de voûte** stone-age (récipient de l'eau C3, four du feu C4, creuset du métal, brique). L'argile restait muette ET le crate Rust réservait un `Mineral::FineClay` **orphelin** depuis Wave 43. C5 le **ferme** : nouveau minéral catalogue `fine_clay` (kaolinite), hiérarchie brique (`shale`) < céramique (`fine_clay`), porte de plasticité d'Atterberg « trop sec → mouiller / plastique → façonner / trop humide → drainer » (pendant inversé de la porte d'humidité de C4), tell kaolin `(180,140,110)` byte-exact ⇔ `Mineral::FineClay`, `PY_TO_RUST` enrichi. Géologie 76→77, global ~79,7 %, pytest 488/488 (+1 skip), smoke `p137` 7/7, seed `0xC1A7`. Antérieur — **Cap. C4 `combustible_outcrop`** : 4ᵉ capacité agent, découverte émergente du **combustible** (tourbe / charbon / schiste bitumineux) — la branche **organique** de la géologie, jusqu'ici muette, devient perceptible (exposition noire-mate + humidité), grade calorifique + porte d'humidité « couper→sécher→brûler », tell charbon `(20,20,20)` byte-exact ⇔ Rust `Mineral::Coal`, garde-fou `PY_TO_RUST` enrichi. Géologie 75→76, global ~79,6 %, pytest 467/467 (+1 skip), smoke `p136`. Antérieur même jour — **décision D5** : `genesis-geology` orphelin tranché par **ADR-0007** (Accepted, option (a) scindée) ; garde-fou cross-langage Python↔Rust livré — fige l'enum `Mineral` (16), le vocabulaire « tell » `PY_TO_RUST` et le tell cuivre/malachite `(80,140,70)` byte-exact ; D6 passe de « protocole non documenté » à « contrat CI-enforced ». Antérieur : Wave 63 concavité de chenal / χ-steepness loi de Flint + méthode χ de Perron-Royden ; Wave 62 hypsométrie Strahler 1952 ; Wave 61 flexure lithosphérique)  
**Synthèse courte** pour contributeurs et reviewers GitHub. Pour le détail session par session, voir [`NEXT-SPRINT.md`](NEXT-SPRINT.md).

---

## En une phrase

**EMERGENCE SIM v2** — laboratoire ZERO PRE-SCRIPT : lois physiques L0–L4, agents autonomes, civilisation **non scriptée**, observable via Earth Console et métriques d'émergence.

**Tests :** `pytest runtime/tests` — **634** tests · smokes **p72–p144** (+ `p87` observer) dans `validate-all`.  

> **Sémantique du score réalisme (ADR-0008, R-J4-1).** Le pourcentage « réalisme »
> ci-dessous mesure la **couche perception agent (Python, `runtime/engine/`)**, **pas**
> le worldgen Rust `native/world-engine/` — gelé depuis Wave 42, `cargo` absent de
> l'env (cf. [ADR-0008](adr/0008-python-rust-frontier.md)). Lire « ~80 % » comme
> « le moteur Rust est à 80 % » serait faux : c'est la qualité de ce que l'agent
> *perçoit* du monde, pas de ce que le moteur *génère*.  

> **Session 2026-06-16 (J+6) — 8ᵉ _capacité_ agent et 1ʳᵉ _transformation_ : trempe thermique de la pierre.** `engine.lithic_tempering` (Cap. C8). Conformément à la recommandation **J+5 §7** de l'audit (« **(b)** `crates/STATUS.md` *puis* **(a)** première capacité de TRANSFORMATION, pas perception, par composition pure »), les deux sont livrés. C1→C6 *montrent* une matière, C7 *amorce* un feu ; **C8 change une propriété** : `base_quality`→`tempered_quality`. **Combo veille du jour** : *ARYA — Physics-Constrained Composable & Deterministic World Model* (arxiv 2603.21340, PAPER_DU_JOUR) dont la thèse (déterminisme + composition sous contrainte physique) **valide** la règle « transformation = composition de capacités déjà ground-truthées, 0 nouveau primitive ». **Physique :** chauffer lentement (~250–400 °C) une silice cryptocristalline (silex/chert) déshydrate le gel de silice → fracture conchoïdale nettement plus régulière (débitage plus facile, bords plus nets) — **plus ancienne pyrotechnologie connue après le feu** (silcrète à Pinnacle Point ~72 ka, antérieure à la poterie/métallurgie). **Compose** C2 `lithic_outcrop` (pierre + `knap_quality`, incl. amélioration silex/chert `CHERT_BONUS`) × C7 `fire_ignition` (feu faisable) — effet **1+1>2** : trempe possible QUE si **silice réactive ET feu** coexistent (un silex en forêt boréale détrempée n'est pas trempable *ici*). **Quatre réponses honnêtes** : silex/chert **+0,20** (matière reine du traitement thermique) / quartzite macrocristallin +0,12 (modeste) / **obsidienne 0** — *le mensonge rendu visible* : déjà du verre volcanique, c'est la *meilleure* pierre à tailler (1,0) et semble la candidate idéale, mais la chauffer ne l'améliore pas (et risque de la fendre) / non-silice (basalte) 0. Outcome **déterministe et ground-truthé** (`tempered_quality` SSOT, borné par `TEMPER_CEILING`=0,95 : même l'excellent chert chauffé reste sous l'obsidienne fraîche). Invariant **« le monde ne ment jamais »** (cue ⇒ `temperable` : pierre réactive réelle C2 dans la *même* colonne que `mine_at` + feu faisable C7) prouvé sur monde Genesis réel **prairie** (seed `0xBEEF`, **84/144 chunks temperables = 76 chert + 8 quartzite, 0 violation**) + boucle **silex+foyer se trempe (+0,20) / obsidienne+foyer vue idéale mais aucun gain** (`temper_preview` non mutant, mensonge rendu visible). **Émergence absolue** (on rend détectable *que cette pierre-ci, chauffée, deviendrait taillable à tel point* ; jamais « chauffe ton silex pour un meilleur outil » — four, enfouissement, durée, refroidissement lent émergent). **Capacité, pas observateur** : aucun hook `sim.step`, coût tick nul → conforme au moratoire. **Garde-fou D8 respecté par COMPOSITION (2ᵉ démonstration après C7)** : pas de `_PROFILE`, **aucune** entrée `PY_TO_RUST`/`PY_CATALOGUE_ONLY`, fichier **hors glob** `*_outcrop.py` — il *lit* C2+C7, décision asservie par `test_introduces_no_new_tell` ; `PY_TO_RUST` reste à **15 entrées**. **Aussi livré (R1 BLIND-SPOTS, dette J+30)** : `native/world-engine/crates/STATUS.md` — **23 crates** classées par inspection source (cargo absent → CI = vérité) : 20 active / 2 entrypoints (`pybindings`, `studio`) / 1 dormant (`gpu` feature-gated) / 1 orpheline (`geology` = oracle ADR-0007), avec ⚠ explicite sur `scenario` (compile non vérifiable ici ; audit 2026-06-09 le notait cassé). 16 tests + smoke `p140` (7/7), pytest **552/552**. **Gap honnête :** la cinétique du four (rampe, durée, refroidissement lent), le risque de sur-chauffe et la perte de matière ne sont pas simulés (affordance + outcome borné, pas une thermodynamique) ; ne ferme aucun item Rust Phase A/B. Détail : [`docs/sprints/2026-06-16_CAP-C8_lithic_tempering.md`](docs/sprints/2026-06-16_CAP-C8_lithic_tempering.md) · veille : [`docs/veille/2026-06-16_VEILLE_lithic_tempering.md`](docs/veille/2026-06-16_VEILLE_lithic_tempering.md).
>
> **Session 2026-06-15 (J+5, suite) — 7ᵉ _capacité_ agent : amorçage du feu (la voûte de l'arc C1→C6).** `engine.fire_ignition` (Cap. C7). C1→C6 ont rendu *perceptibles* les **matières** de l'âge de pierre, mais presque toutes demandent ensuite *un feu* pour devenir outil (fondre le cuivre C1, brûler le combustible C4, cuire l'argile en céramique C5, calciner le calcaire en chaux C6). Sans amorçage *par l'agent*, ces capacités restaient des matières inertes : C7 est **la voûte qui les rend actionnables**. `engine.wildfire` (Wave 14) modélise le feu **spontané** (foudre + propagation Rothermel) et note lui-même que l'agent doit *déduire* que « le silex frappé produit la même chose en petit » — mais aucun signal de substrat ne disait, *par site*, si un humain peut allumer un feu ici et comment. Ajout de l'**affordance d'amorçage anthropique** via un **combo veille du jour** à **deux voies physiques honnêtes** : **PERCUSSION** (briquet à pierre) — pyrite (FeS₂ *pyrophorique* : l'éclat arraché s'oxyde en jetant une étincelle) frappée par un percuteur dur (silex/quartz, **pétrologie C2 réutilisée verbatim** incl. amélioration silex/chert) sur un amadou *assez* sec (≤ 0,58) ; **FRICTION** (archet/drille) — sans minéral, mais amadou *très* sec (≤ 0,45, un amadou humide tue la braise). La veille **confirme** la physique : pyrite+silex = la **plus ancienne production de feu connue** (site néandertalien de Beeches Pit ~400 ka : sol brûlé + silex fire-cracked + 2 fragments de pyrite ; méthode d'Ötzi ~3300 av. J.-C. : silex+pyrite+amadou de polypore) ; flammabilité gouvernée par l'humidité du combustible (feux de prairie à FMC < 35 %). Effet 1+1>2 : géologie (pyrite+silex, SYSTÈME C) × hydrologie de surface (`chunk.water`, SYSTÈME A) × biome combustible (SYSTÈME E) — une seule vérité de substrat, lue par C4 (sec), C5 (plastique), C6 (sain), C7 (amadou sec). Invariant **« le monde ne ment jamais »** (site ⇒ `can_ignite` ; percussion ⇒ pyrite réelle peu profonde dans la *même* colonne que `mine_at` + percuteur réel + amadou sec ; friction ⇒ amadou sec & combustible) prouvé sur monde Genesis réel **prairie** (seed `0xBEEF`, 144 chunks ignitables = 61 percussion + 144 friction, 0 violation) + boucle **prairie sèche pyrite+silex s'allume / prairie détrempée vue comme amadou mais ne prend pas** (`ignition_preview` non mutant, mensonge rendu visible). **Émergence absolue** (on rend la pierre-à-étincelle, le percuteur et la sécheresse de l'amadou détectables, jamais « frappe la pyrite pour faire du feu »). **Capacité, pas observateur** : aucun hook `sim.step`, coût tick nul → conforme au moratoire. **Garde-fou D8 respecté par COMPOSITION, pas contourné** : contrairement à C2/C4/C5/C6, C7 **n'introduit aucun nouveau tell minéral** (pas de `_PROFILE`, pas d'entrée `PY_TO_RUST`/`PY_CATALOGUE_ONLY`, fichier hors glob `*_outcrop.py`) — il *réutilise* la pyrite (gossan C1) + le percuteur (C2), décision consciente asservie par `test_introduces_no_new_tell`. **N'aggrave donc pas le treadmill** que l'audit J+4 craignait (pas de 7ᵉ minerai muet parallèle ; débloque l'actionnabilité des 6 précédents). 20 tests + smoke `p139` (7/7), pytest **536/536**. **Gap honnête :** la cinétique de la combustion, l'entretien du foyer et le passage amadou→bois ne sont pas simulés (affordance binaire d'amorçage, pas une flamme physique) ; le biome donne le *load* d'amadou (statique), pas une biomasse dynamique ; ne ferme aucun item Rust Phase A/B. Détail : [`docs/sprints/2026-06-15_CAP-C7_fire_ignition.md`](docs/sprints/2026-06-15_CAP-C7_fire_ignition.md) · veille : [`docs/veille/2026-06-15_VEILLE_fire_ignition.md`](docs/veille/2026-06-15_VEILLE_fire_ignition.md).
>
> **Session 2026-06-15 (J+5) — décision de frontière (ADR-0008) + garde-fou D8, pas de Cap. C7.** Conformément à la recommandation **J+5 (b)** de l'audit J+4 (choix exclusif : (a) C7 ou (b) trancher la frontière Python/Rust → recommandait (b)), on **lève le plus grand silence stratégique du projet** au lieu d'empiler une 7ᵉ capacité. **[ADR-0008](adr/0008-python-rust-frontier.md) (Accepted)** nomme la frontière, **réversible sous conditions** : (1) `runtime/engine/` (Python déterministe) = **couche de simulation/perception active** de l'ère *cargo-less* (constat assumé : 516 tests verts, émergence intacte) ; (2) `native/world-engine/` = **substrat worldgen gelé** + oracle de contrat (ADR-0007), items Phase A/B **différés** (pas abandonnés) à une « session cargo » ; (3) frontière = le contrat de *tells* géologie, **durci** ci-dessous ; (4) **score honnête (R-J4-1)** : le % réalisme mesure la perception Python, pas le worldgen Rust (caveat ajouté ci-dessus) ; (5) conditions de réactivation Rust listées (R-J4-2, D5-wiring, A3/A4/A5). **Garde-fou D8 (R-J4-3)** dans `test_geology_cross_language_contract.py` : ferme **F-D8-2** — auto-découverte des `_PROFILE` des 4 capacités `*_outcrop` + nouveau waiver documenté `PY_CATALOGUE_ONLY` (slate/shale/basalt/gneiss/granite/sandstone + carbonates limestone/calcite/marble/dolomite) → **tout tell surfacé doit être classé** (mappé `PY_TO_RUST` ou waivé) ET **tout `engine/*_outcrop.py` doit être enregistré**, sinon **le build casse**. Le moratoire CONTRIBUTING.md passe de **règle sociale** à **porte CI** : C7 ne pourra plus diverger en silence. **Émergence absolue** (0 contenu scripté), **capacité ? non — garde-fou** (0 hook `sim.step`, coût tick nul). 3 tests, **pytest 516/516** (513→516), 0 skip. **Gap honnête :** F-D8-1 (parsing texte du Rust) non fermé ici — demande le binding compilé `mineral_tells` (R-J4-2), donc `cargo` → différé ADR-0008 §5 ; D7 (0 commit Rust) inchangé mais désormais **choix daté**, pas stagnation muette. Détail : [`docs/sprints/2026-06-15_ADR0008_frontier_and_D8_guardrail.md`](docs/sprints/2026-06-15_ADR0008_frontier_and_D8_guardrail.md) · veille : [`docs/veille/2026-06-15_VEILLE.md`](docs/veille/2026-06-15_VEILLE.md).
>
> **Session 2026-06-14 (J+4, suite) — 6ᵉ _capacité_ agent : affleurement calcaire.** `engine.limestone_outcrop` (Cap. C6). C5 a livré l'**argile** (le matériau qui *contient* : récipient/four/creuset/brique) ; son **pendant construction** est le **calcaire** (le matériau qui *bâtit & colle* : pierre de taille + chaux qui lie les pierres). La veille du jour ancre le maillon dans l'âge de pierre : la **chaux est le plus ancien liant connu** — néolithique (sols d'enduit à Göbekli Tepe ~9500 av. J.-C.), **antérieur à la métallurgie**. Or le calcaire restait **muet** (lithologie `limestone` + ores carbonatés non perceptibles) et le crate Rust `genesis-geology` réservait depuis Wave 43 un `Mineral::LimestonePure` (« Quicklime precursor — pure carbonate beds », `[245,240,225]`) **orphelin**. Ajout de la lecture d'**affleurement carbonaté** via un **combo veille** à **deux propriétés honnêtes et orthogonales** : D1 **grade de chaux** (pureté carbonatée → décarbonatation 700–900 °C → chaux vive ; seul `limestone_pure`/`calcite`/`marble` atteignent `mortar_grade` ; `limestone`/`dolomite` = pierre à bâtir + chaux faible) × D3 **porte d'altération** (karstification : exposition sèche/tempérée `sound_quarry` dressable / humide `karst_fissured` / gelante `frost_shattered` — lien **Wave 50** ; trois états exclusifs/exhaustifs). Effet 1+1>2 : hydrologie (SYSTÈME A) × géologie (SYSTÈME C) × gel (Wave 50) ; `mortar_grade` (chaux) et `dressable_now` (blocs) **indépendants** (un calcaire pur fissuré brûle en mortier mais ne se dresse pas). Invariant **« le monde ne ment jamais »** prouvé sur monde Genesis réel (seed `0xC1A7`, 144 chunks = 123 calcaire pur + 21 calcaire commun, 144 dressables, 123 mortier, 0 violation) + boucle **calcaire pur sain se dresse & brûle en chaux / falaise karst-fissurée vue mais infaçonnable en blocs** (`work_preview` non mutant, mensonge rendu visible). **Émergence absolue** (on rend le carbonate, sa pureté et son altération détectables, jamais « construis un mur »). **Capacité, pas observateur** : aucun hook `sim.step`, coût tick nul → conforme au moratoire. **Garde-fou ADR-0007 honoré + orphelin fermé** : nouveau `limestone_pure` (CaCO3 pur) au catalogue (appendu → `MINERAL_INDEX` stable), `PY_TO_RUST` enrichi `limestone_pure → LimestonePure` (sorti de `RUST_ONLY`), **verrou byte-exact** du tell `(245,240,225)` ⇔ `Mineral::LimestonePure::surface_color()` (**4ᵉ couleur verrouillée** après malachite, charbon & fine_clay). 24 tests + contrat cross-langage +1 + smoke `p138` (7/7), pytest **513/513**. **Gap honnête :** la cinétique de calcination et le cycle de la chaux (CaO→Ca(OH)₂→CaCO₃) ne sont pas simulés ; la distinction chaux aérienne/hydraulique non plus (seuils, pas un continuum) ; ne ferme aucun item Rust Phase A/B. Détail : [`docs/sprints/2026-06-14_CAP-C6_limestone_outcrop.md`](docs/sprints/2026-06-14_CAP-C6_limestone_outcrop.md) · veille : [`docs/veille/2026-06-14_VEILLE_limestone_outcrop.md`](docs/veille/2026-06-14_VEILLE_limestone_outcrop.md).
>
> **Session 2026-06-14 (J+4) — 5ᵉ _capacité_ agent : affleurement d'argile.** `engine.clay_outcrop` (Cap. C5). C1 a livré le minerai, C2 la pierre, C3 l'eau, C4 le combustible ; la docstring de C4 désignait le maillon suivant : « feu durable → **four en argile** + charbon → fusion ». Or l'**argile** — la **clé de voûte** stone-age (le récipient qui contient l'eau C3, le four qui contient le feu C4, le creuset qui contient le métal, la brique qui bâtit) — restait **muette**, et le crate Rust `genesis-geology` réservait depuis Wave 43 un `Mineral::FineClay` **orphelin** (« no catalogue mineral of this name », noté au contrat cross-langage). Ajout de la lecture d'**affleurement d'argile** que tout potier néolithique sait faire, via un **combo veille** : D1 **hiérarchie de grade** (argile schisteuse `shale` *brique* < kaolin `fine_clay` *céramique* ; seul le kaolin atteint `ceramic_grade`, vitrifie étanche, tient un creuset) × D2 **porte de plasticité** (limites d'Atterberg 1911 : `too_dry_to_shape` < fenêtre `workable_now` < `too_wet_slurry` → boucle émergente mouiller/sécher avant de façonner). Effet 1+1>2, **pendant inversé de C4** : le feu veut le sec, l'argile veut le plastique — une seule vérité de substrat (biome + `chunk.water`), deux lectures. Invariant **« le monde ne ment jamais »** (cue ⇒ argile réelle peu profonde dans la *même* colonne que `mine_at` ; `workable_now` ⇒ humidité dans PL→LL ; `ceramic_grade` ⇒ grade ≥ seuil ; trois états d'humidité exclusifs/exhaustifs) prouvé sur monde Genesis réel (seed `0xC1A7`, 144 chunks = 123 kaolin + 21 schiste, 0 violation) + boucle **kaolin plastique se façonne & cuit en céramique / argile désertique sèche vue mais infaçonnable** (`shape_preview` non mutant, mensonge rendu visible). **Émergence absolue** (on rend l'argile et sa plasticité détectables, jamais « fais un pot »). **Capacité, pas observateur** : aucun hook `sim.step`, coût tick nul → conforme au moratoire. **Garde-fou ADR-0007 honoré + orphelin fermé** : nouveau `fine_clay` (kaolinite) au catalogue (appendu → `MINERAL_INDEX` stable), `PY_TO_RUST` enrichi `fine_clay → FineClay` (sorti de `RUST_ONLY`), **verrou byte-exact** du tell kaolin `(180,140,110)` ⇔ `Mineral::FineClay::surface_color()` (3ᵉ couleur verrouillée après malachite & charbon). 20 tests + contrat cross-langage +1 + smoke `p137` (7/7), pytest **488/488** (+1 skip). **Gap honnête :** la diagenèse argile→schiste→ardoise et la levigation ne sont pas simulées (grades = matériaux distincts) ; ne ferme aucun item Rust Phase A/B. Détail : [`docs/sprints/2026-06-14_CAP-C5_clay_outcrop.md`](docs/sprints/2026-06-14_CAP-C5_clay_outcrop.md) · veille : [`docs/veille/2026-06-14_VEILLE_clay_outcrop.md`](docs/veille/2026-06-14_VEILLE_clay_outcrop.md).
>
> **Session 2026-06-13 (J+3, suite) — 4ᵉ _capacité_ agent : affleurement de combustible.** `engine.combustible_outcrop` (Cap. C4). Toute la branche **ORGANIQUE** de la géologie (`peat`/`coal`/`oil_shale`, déjà semée dans l'`ore_mix` par `engine.geology`) restait **muette** : aucun signal de surface ne disait *où trouver la roche/terre qui brûle*. Or « la roche noire mate qui brûle longtemps » amorce la **révolution énergétique** du prompt (SYSTÈME F : feu durable → four + charbon → fusion → métallurgie). Ajout de la lecture de l'**exposition de combustible** (noir mat + humidité) via un **combo veille** : D1 **rang houiller** (grade calorifique tourbe<schiste<charbon ; seul le charbon atteint `smelting_grade`) × D2 **porte d'humidité** (moisture-of-extinction de Rothermel : une tourbière gorgée d'eau est *vue* mais `dry_to_burn`, pas `burnable_now` → boucle émergente couper→sécher→brûler). Effet 1+1>2 : géologie organique (SYSTÈME C) reliée à l'hydrologie de surface (SYSTÈME A). Invariant **« le monde ne ment jamais »** (cue ⇒ combustible réel peu profond dans la *même* colonne que `mine_at` ; `burnable_now` ⇒ grade & sec ; `smelting_grade` ⇒ grade ≥ seuil) prouvé sur monde Genesis réel **boréal** (seed `0xB0`, 66/144 chunks = 24 charbon + 42 tourbe, 0 violation) + boucle **charbon sec tient le feu & fond le métal / tourbe gorgée d'eau vue mais à sécher** (aperçu `ignite_preview` non mutant, mensonge rendu visible). **Émergence absolue** (on rend l'exposition carbonée et son humidité détectables, jamais « ceci brûle »). **Capacité, pas observateur** : aucun hook `sim.step`, coût tick nul → conforme au moratoire. **Garde-fou ADR-0007 honoré** : `PY_TO_RUST` enrichi (surface enfin `coal`, ajoute `peat`/`oil_shale`) + **verrou byte-exact** du tell charbon `(20,20,20)` ⇔ `Mineral::Coal::surface_color()` (miroir du tell cuivre/malachite). 18 tests + contrat cross-langage +1 + smoke `p136` (7/7), pytest **467/467** (+1 skip). **Gap honnête :** la diagenèse tourbe→charbon (houillification) n'est pas simulée (rangs = matériaux distincts du catalogue) ; ne ferme aucun item Rust Phase A/B. Détail : [`docs/sprints/2026-06-13_CAP-C4_combustible_outcrop.md`](docs/sprints/2026-06-13_CAP-C4_combustible_outcrop.md) · veille : [`docs/veille/2026-06-13_VEILLE_combustible_outcrop.md`](docs/veille/2026-06-13_VEILLE_combustible_outcrop.md).
>
> **Session 2026-06-13 (J+3) — DÉCISION D5 + garde-fou géologie cross-langage (anti-divergence, pas de Cap. C4).** Au lieu d'une 4ᵉ capacité (interdite par l'audit tant que D5 n'était pas tranché), on **ferme le risque #1 du moteur**. Les delta-audits avaient escaladé **D5** (`native/world-engine/crates/geology`, 1095 lignes Rust, **orphelin** — importé par personne) et **D6** (3 capacités C1/C2/C3 dérivent la géologie côté Python pendant que la crate Rust dort → *double source de vérité, protocole non documenté, « sans test cross-langage rien ne le garantit »*). Décision prise et datée → **[ADR-0007](adr/0007-d5-geology-orphan-resolution.md) (Accepted)** : option (a) **scindée** — (1) verrou de contrat livré aujourd'hui (exécutable **sans `cargo`**), (2) câblage moteur Rust déféré à une session CI (item Phase A « D5-wiring »). Le garde-fou `runtime/tests/test_geology_cross_language_contract.py` parse `crates/geology/src/mineral.rs` comme **oracle lecture-seule** et fige : l'identité de l'enum `Mineral` (16 variantes + `MINERAL_COUNT`), le mapping documenté `PY_TO_RUST` des minéraux « tell » (nom vérifié des deux côtés), le **tell cuivre/malachite `(80,140,70)` byte-exact** (`surface_mineralization` ⇔ `Mineral::Malachite::surface_color()`), et le contrat intra-Python sel (croûte C1 == saumure C3, claimé en commentaire, désormais exécuté). Toute dérive d'un côté **casse le build** → la divergence linéaire que D6 projetait (~4000 lignes à C10) est rendue impossible. **Moratoire C4 levé par garde** : toute future capacité doit enrichir `PY_TO_RUST`. **Émergence intacte** (aucun arbre tech, aucun hook `sim.step`, coût tick nul) → conforme moratoire. 7 tests, pytest **448/448** (+1 skip pré-existant). **Gap honnête :** contrat vérifié par parsing texte du Rust, pas binding compilé ; le câblage Rust réel (étape 2) reste dû — seule la dette de *divergence* est fermée, pas celle de *câblage*. Détail : [`adr/0007-d5-geology-orphan-resolution.md`](adr/0007-d5-geology-orphan-resolution.md) · veille : [`docs/veille/2026-06-13_VEILLE_D5_geology_contract.md`](docs/veille/2026-06-13_VEILLE_D5_geology_contract.md).
>
> **Session 2026-06-12 (suite) — 3ᵉ _capacité_ agent : potabilité de l'eau.** `engine.water_potability` (Cap. C3). La ressource **la plus fondamentale de toutes** — l'eau potable (on meurt de soif avant la faim, avant l'outil) — restait muette **d'une façon physiquement fausse** : `engine.physiology` (action `DRINK`) réduit la soif pour **n'importe quelle** cellule d'eau, **y compris l'eau de mer** → le monde laissait un agent « boire l'océan » et être hydraté. Ajout de la lecture de **salinité** par le goût (bandes WHO/EPA : douce < 0,5 ppt / saumâtre / mer 35 ppt / saumure), dérivée de **vérités indépendantes** : biome `OCEAN` (mer), **halite** peu profonde en `chunk_geology` (saumure — la *même* couche que la croûte de sel de C1), basse élévation (mélange estuarien côtier), sinon eau douce (dureté carbonatée). Invariant **« le monde ne ment jamais »** (potable ⇒ ≠ OCEAN & pas de saumure & ppt ≤ seuil ; mer ⇒ OCEAN ; saumure ⇒ halite) prouvé sur monde Genesis (seed `0xFACE`, 0 violation) + boucle **goûter douce → `drink_at` hydrate** / **océan injecté → perçu salé → n'hydrate pas** (aperçu non mutant, le mensonge rendu visible). **Émergence absolue** (on rend la salinité détectable, jamais « ne bois pas »). **Capacité, pas observateur** : aucun hook `sim.step`, coût tick nul → conforme au moratoire. **Perception-seule** : on n'altère pas `DRINK` (changement comportemental hors moratoire). 15 tests + smoke `p135` (7/7), ADR-0005 audité `ok`, pytest **441/441**. **Gap honnête :** la nappe phréatique (groundwater/Darcy) n'est pas modélisée (eau = champ de surface) ; ne ferme aucun item Rust Phase A/B. Détail : [`docs/sprints/2026-06-12_CAP-C3_water_potability.md`](docs/sprints/2026-06-12_CAP-C3_water_potability.md) · veille : [`docs/veille/2026-06-12_VEILLE_water_potability.md`](docs/veille/2026-06-12_VEILLE_water_potability.md).
>
> **Session 2026-06-12 — 2ᵉ _capacité_ agent (anti-treadmill) : pierre taillable.** `engine.lithic_outcrop` (Cap. C2). Pendant de C1, technologie **plus fondamentale encore** : la géologie portait la lithologie (`StrataLayer.rock_type`) et les silicates taillables (`obsidian`/`quartz` dans `ore_mix`) mais restait **muette** — aucun signal ne disait *où trouver une pierre qui fait des lames tranchantes*. Ajout de la lecture d'**affleurement** que tout tailleur paléolithique sait faire : classe de fracture (`CONCHOIDAL`/`TABULAR`/`GROUND`/`SOFT`), hiérarchie archéologique **obsidienne > silex(chert) > quartzite > basalte > granite**, silex modélisé comme `quartz` bonifié en hôte carbonaté, affleurement vs enfouissement (socle ≤ 6 m). Invariant **« le monde ne ment jamais »** (cue ⇒ pierre réelle dans la même colonne que `mine_at`) prouvé sur monde Genesis (seed `0xFACE`, 0 violation) + boucle **voir verre → débiter → obsidienne** end-to-end. **Émergence absolue** (on rend l'affleurement détectable, jamais « ceci taille bien »). **Capacité, pas observateur** : aucun hook `sim.step`, coût tick nul → conforme au moratoire. 15 tests + smoke `p134` (7/7), ADR-0005 lint vert, pytest **426/426**. **Gap honnête :** la provenance volcanique de l'obsidienne n'est pas verrouillée (à traiter côté `engine.geology`) ; ne ferme aucun item Rust Phase A/B. Détail : [`docs/sprints/2026-06-12_CAP-C2_lithic_outcrop.md`](docs/sprints/2026-06-12_CAP-C2_lithic_outcrop.md).
>
> **Session 2026-06-11 (suite) — 1ʳᵉ _capacité_ agent depuis 25 j (anti-treadmill, audit §D1/§3) :** `engine.surface_mineralization`. La géologie semait des minerais en profondeur mais le monde restait **muet** : aucun signal de surface ne permettait à un agent de *découvrir par la vue* un gisement (l'action `MINE` partait d'une profondeur par défaut, à l'aveugle). Ajout du **chapeau de fer / tache d'altération** des prospecteurs (gossan limonite-brun, malachite-vert cuivre, soufre-jaune, sel-blanc, placer-doré), **dérivé de la même colonne `chunk_geology` que `mine_at`** → invariant **« le monde ne ment jamais »** (cue ⇒ minerai peu profond réel) prouvé sur monde Genesis réel (seed `0xFACE`, 100 % chunks, 0 violation) ; boucle de découverte **voir vert → creuser → cuivre** vérifiée end-to-end. **Émergence absolue respectée** (on rend la couleur détectable, on ne dit jamais « c'est du cuivre »). **Capacité, pas observateur** : aucun hook `sim.step`, coût tick nul → conforme au moratoire. 15 tests + smoke `p133` (7/7), ADR-0005 lint strict vert. **NB honnêteté audit :** ne ferme **aucun** item Rust Phase A/B (A3/A4/A5/B1–B8 restent ouverts, `cargo` absent de l'env → CI = vérité) ; c'est une capacité du **runtime Python live**, distincte du backlog moteur Rust.
>
> **Session 2026-06-11 (antérieur) — rupture du « treadmill d'observateurs » (audit §D1) :** aucune Wave 64. (1) **Pont Python↔Rust réactivé** — le contrat trop strict (`is_canonical_pyworld` seul) rejetait silencieusement le wheel `ge-py` (surface *terrain*) vers `MockPyWorld` → backend natif inactif depuis Wave 42. Fix `rust_bridge.py` (`is_terrain_pyworld` / `is_native_pyworld`) **vérifié en live** : `bridge_status()` → `{native: True, backend: "terrain"}`. (2) **Garde D1** `engine/observer_budget.py` + `tests/test_observer_budget.py` (idempotence cross-observer prouvée, budget tick < 10 % instrumenté) + moratoire formalisé dans `CONTRIBUTING.md`. Diffs Rust moteur (`manager.rs` fix course mutation/éviction, `scenario` dép `worldgraph`, `-D warnings`) validés par inspection — **cargo absent de l'environnement**, CI = source de vérité.  
**CI :** le job Python exécute `make doctor`, `compile-python`, `test-python`, puis les smokes réalisme dans le **même ordre que `make validate-all`**, puis `p82_observation_sse_smoke.py` (observation SSE).

### Philosophie — émergence civilisationnelle

Les phénomènes (climat Köppen, hydrologie, épidémies, commerce, culture, observation) **émergent du cycle `Simulation.step()`** et des interactions agents ↔ monde — pas d’un orchestrateur de scripts qui enchaîne des étapes. Les smokes (`scripts/p*.py`) et `make smoke` **valident** le comportement ; le cœur exécutable est `python runtime/run.py` ou une boucle `sim.step()`.

**Manifeste (source de vérité) :** [`docs/EMERGENCE-SIM-v2.md`](docs/EMERGENCE-SIM-v2.md) · **Master prompt agents :** [`docs/MASTER-SCALE-PROMPT-v2.md`](docs/MASTER-SCALE-PROMPT-v2.md) · KPIs : `engine/emergence_metrics.py` · `/api/emergence_metrics`

**Presets :**
- **`python runtime/run.py realism`** — biosphère + knowledge layers + hydrologie sv1d + genesis + rust WorldGraph tick + 5cd (`engine/full_stack.py`).
- **`python runtime/run.py terre`** — fusion **origins** + **realism** (founders émergents, 2000 ticks par défaut).

---

## Avancement par phase (roadmap produit)

| Phase | Intitulé | Statut | Notes |
|-------|----------|--------|-------|
| **0** | Foundations (ECS, monorepo, observabilité) | ✅ | ADR-0005, PRF, CI (smokes alignés `validate-all`) |
| **1** | MVP Vie (cognition, mort biologique) | ✅ | Boucle perceive → decide → act |
| **2** | MVP Société (reproduction, mémoire, lexique) | ✅ | Multi-générations, vocalisations |
| **3** | MVP Civilisation (construction, troc, conflits) | 🟡 | Partiel ; Waves 28–32 enrichissent |
| **4** | Émergence civilisationnelle | ✅ | Agriculture, écriture, polity, métallurgie |
| **5** | Genesis-α public (long-run, LLM tier-2) | ⏳ | 9/10 prérequis livrés — **plan d'unlock Wave 12 figé le 2026-05-23** (cf. `../Genesis_Engine_2026-05-23_Phase5_Unlock_Brief.md`, GO/NO-GO J+10 = 2026-06-02) |
| **Waves 16–41** | Monde réaliste intégré au tick | ✅ | Genesis, climat, hydrologie, observateurs dans `sim.step()` |
| **Biosphère émergente** | Protocellules → sapients | ✅ | [`docs/BIOSPHERE-EMERGENCE.md`](docs/BIOSPHERE-EMERGENCE.md) · `run.py origins` |

Détail des **Waves 16–41** (genesis, tectonique, climat, NCA, settlements, routes, épidémie, iso, atmosphère…) : index dans [`docs/sprints/README.md`](docs/sprints/README.md) (fichiers `2026-05-18_WAVE*.md`).

---

## Réalisme Terre (grille scientifique)

**Score global : ~80,2 %** (moyenne 7 dimensions = (80+78+74+79+82+86+82)/7 = 80,1 % — **cible 80 % dépassée** — Sociétés/agents 78→79 via l'arc pyrotechnologique C7→C12 mûri : **Cap. C12 tirage forcé** (soufflet + charbon de bois) **RÉALISE** la vitrification (`vitrifies_watertight` enfin True pour le kaolin réfractaire) et **OUVRE** la métallurgie du cuivre (`would_smelt_copper_here`, ≥1085 °C × tell vert C1), **aucun nouveau tell** (composition C11×C1, garde-fou D8 par composition 6ᵉ fois) ; et **Cap. C11 four à tirage** + **C9/C10 cuisson céramique/chaux**. Antérieur 80,0 % — Sociétés/agents 77→78 via **Cap. C7 amorçage du feu** : la **voûte qui rend l'arc C1→C6 actionnable** — deux voies physiques (PERCUSSION pyrite+silex / FRICTION) ; **n'introduit aucun nouveau tell** (composition de la pyrite-gossan C1 + percuteur C2, garde-fou D8 par composition) ; invariant « le monde ne ment jamais ». Antérieur 79,9 % — Géologie/relief 77→78 via **Cap. C6 affleurement calcaire** : le pendant construction de l'argile (pierre de taille + chaux/mortier) devient perceptible — grade de chaux (pureté → mortier) × porte d'altération (sain/karst/gel), ferme l'orphelin Rust `LimestonePure`, tell `(245,240,225)` byte-exact. Antérieur 79,7 % — Géologie/relief 76→77 via **Cap. C5 affleurement d'argile** : la clé de voûte stone-age (récipient/four/creuset/brique) devient perceptible — hiérarchie brique/céramique + porte de plasticité d'Atterberg, ferme l'orphelin Rust `FineClay`, tell `(180,140,110)` byte-exact. Antérieur 79,6 % — Géologie/relief 75→76 via **Cap. C4 affleurement de combustible** : la branche organique du substrat (tourbe/charbon/schiste bitumineux) devient perceptible — grade calorifique + porte d'humidité « couper→sécher→brûler » + tell charbon `(20,20,20)` byte-exact, découverte d'énergie émergente, invariant « le monde ne ment jamais ». Antérieur 79,4 % — Écologie/hydrologie 73→74 via **Cap. C3 potabilité de l'eau** : première _capacité_ de découverte d'**eau potable** émergente — la salinité (douce/saumâtre/mer) devient perceptible et véridique, comblant une muette physiquement fausse de `physiology.DRINK` ; antérieur 79,3 % Cap. C2 Sociétés 76→77 affleurements de pierre taillable ; 79,1 % Cap. C1 géologie 74→75 indices de surface minéralisée). **Objectif cible : 80 %**. _NB (audit §3) : ce score mesure le réalisme **observé** ; le sous-score « capacité moteur Rust » (A3/A4/A5/B1–B8) reste à **0/7** — non touché par cette capacité Python.

| Dimension | % | Piste principale |
|-----------|---|------------------|
| Climat / biomes | 80 | GraphCast-lite + colonne 3D + circulation L1 + vent 2D |
| Géologie / relief | 77 | Tectonique live, stratigraphie + datation relative + absolue (Wave 51) + cryoclastie (Wave 50) + compaction diagénétique (Wave 54) + géotherme/faciès métamorphiques (Wave 56) + lit mobile Exner (Wave 57) + isostasie d'Airy (Wave 59) + flexure lithosphérique élastique (Wave 61) + hypsométrie / maturité de paysage Strahler 1952 (Wave 62) + Wave 63 concavité de chenal / χ-steepness ; **Cap. C1 — indices de surface minéralisée** (gossan/malachite/jarosite/placer/sel, oxydation supergène, découverte visuelle émergente, invariant « le monde ne ment jamais ») ; **Cap. C4 — affleurement de combustible** (tourbe/charbon/schiste bitumineux, grade calorifique du rang houiller, porte d'humidité de Rothermel « couper→sécher→brûler », tell charbon `(20,20,20)` byte-exact ⇔ Rust `Mineral::Coal`, découverte d'énergie émergente) ; **Cap. C5 — affleurement d'argile** (kaolin céramique vs schiste brique, porte de plasticité d'Atterberg « mouiller→façonner→sécher→cuire », ferme l'orphelin Rust `FineClay`, tell kaolin `(180,140,110)` byte-exact ⇔ `Mineral::FineClay`, clé de voûte de la poterie/four/creuset émergents) (loi de Flint pente–aire `S = k_s·A^−θ`, méthode χ intégrale de Perron & Royden 2013, invariant pivot = récupération exacte de la loi de puissance + linéarité χ–z, bande gradée θ∈[0,40 ; 0,60]) |
| Écologie / hydrologie | 74 | `hydrology_mode` sv1d ; Earth Console overlay flux ; Wave 49 quantification réseau (Strahler + Horton + drainage density) ; Wave 53 routage de débit LTI ; **Wave 57 boucle eau→sédiment→relief fermée** (Exner sur graphe D8) ; **Cap. C3 — potabilité de l'eau** (salinité douce/saumâtre/mer perçue par le goût, dérivée de biome OCEAN + halite peu profonde + élévation côtière, invariant « le monde ne ment jamais ») |
| Sociétés / agents | 79 | NEAT + construction émergente + memetic + `/api/audio` ; **Cap. C9/C10/C11 pyrotechnologie** (cuisson céramique, calcination de la chaux, four à tirage) ; **Cap. C12 — tirage forcé** (soufflet + charbon : `forced_draught_peak_c` = pointe du four C11 + gain du soufflet plafonné par la paroi → ~1100–1400 °C ; **RÉALISE** la vitrification du kaolin réfractaire `vitrifies_watertight` que C9/C11 différaient — l'arc « mensonge du kaolin » se ferme ; **OUVRE** la métallurgie `would_smelt_copper_here` ≥1085 °C × tell vert C1 ; **aucun nouveau tell**, composition C11×C1, garde-fou D8 par composition 6ᵉ ; invariant « le monde ne ment jamais ») ; **Cap. C2 — affleurements de pierre taillable** (obsidienne/silex/quartzite/basalte, classe de fracture, hiérarchie archéologique, découverte d'outil émergente, invariant « le monde ne ment jamais ») ; **Cap. C7 — amorçage du feu** (la *voûte* qui rend l'arc C1→C6 actionnable : PERCUSSION pyrite pyrophorique FeS₂ + percuteur silex [pétrologie C2] + amadou ≤ 0,58 — la plus ancienne production de feu connue, méthode d'Ötzi — / FRICTION sans minéral, amadou ≤ 0,45 ; combo géologie × hydrologie `chunk.water` × biome combustible ; **aucun nouveau tell** — composé de C1-pyrite + C2-silex ; distinct de `wildfire` (feu spontané) ; invariant « le monde ne ment jamais ») ; **Cap. C8 — trempe thermique de la pierre** (1ʳᵉ _transformation_ : chauffer un silex/chert au foyer améliore sa taille `base_quality`→`tempered_quality`, +0,20 chert / +0,12 quartzite, **obsidienne 0** = déjà du verre, le mensonge rendu visible ; composition C2×C7, **aucun nouveau tell** ; plus ancienne pyrotechnologie après le feu, Pinnacle Point ~72 ka ; invariant « le monde ne ment jamais ») |
| Rendu visuel | 82 | Globe + iso 2.5D + humains + ombres + 2D lite |
| Observation IA | 86 | Earth Console SSE, replay, observer_feed, WebGPU |
| Pont Python ↔ Rust | 82 | GENM + mutations write-back + snapshot |

**Référence unique (source de vérité) :** [`docs/ROADMAP-REALISME-TERRE.md`](docs/ROADMAP-REALISME-TERRE.md)  
**Architecture God-Engine (audit next-level) :** [`docs/GOD-ENGINE-ARCHITECTURE.md`](docs/GOD-ENGINE-ARCHITECTURE.md) · P0 **GENM + align_heightmap** livré (`macro_grid_export.py`, `genesis-macro-bridge`)  
**Prochain sprint réalisme :** section « Prochain sprint » dans ce fichier roadmap.

---

## Stacks actives

| Stack | Chemin | Rôle |
|-------|--------|------|
| **Runtime Python** | [`runtime/`](runtime/README.md) | Simulation agents, civilisation, rendu, smokes `p0`–`p79` |
| **World Engine Rust** | [`native/world-engine/`](native/world-engine/README.md) | WorldGraph, biome Köppen, streaming, GPU érosion (parallèle, pas remplacement) |
| **Scaffolding Rust** | `scaffolding/` | Crates historiques ECS / proto (CI `make rust-check`) |
| **Docs & preuves** | [`docs/`](docs/README.md) | Sprints, compliance renders, roadmap |

---

## Pipeline civilisation (orchestration)

`runtime/scripts/civilization_pipeline.py` enchaîne bootstrap Genesis (**via `wire_full_stack`** :
genesis, rust WorldGraph, 5cd), coupler multi-taux, ticks agents et exports
(observable, Köppen FAIR, épidémie).
Entrée Makefile : **`make civilization`** · smoke : **p82**.

## Smokes de référence (dernière vague)

Depuis la racine du repo (`PYTHONPATH=runtime` implicite via `make` ou `cd runtime`) :

```bash
make smoke                    # p0 — sanity
make civilization             # pipeline émergence + manifest
make validate-all             # pytest (157 tests) + smokes p72–p87 + SSE
make earth-console            # Terre live UI (8090)
make terre-long               # preset terre 2000 ticks + artifact enrichi
python run.py terre --ticks 500
cd runtime && python scripts/p82_civilization_pipeline_smoke.py
```

Liste complète : [`runtime/README.md`](runtime/README.md#smoke-tests).

---

## Fichiers « source de vérité »

| Besoin | Fichier |
|--------|---------|
| File de travail vivante | [`NEXT-SPRINT.md`](NEXT-SPRINT.md) |
| Roadmap phases 0–5 | [`ROADMAP.md`](ROADMAP.md) |
| Réalisme Terre chiffré | [`docs/ROADMAP-REALISME-TERRE.md`](docs/ROADMAP-REALISME-TERRE.md) |
| Spec contractuelle | [`Genesis_Engine_Architecture_v1.0.docx`](Genesis_Engine_Architecture_v1.0.docx) |
| Vision long terme | [`FUTURE-VISION.md`](FUTURE-VISION.md) |
| Contribuer | [`CONTRIBUTING.md`](CONTRIBUTING.md) |

---

## Renders & conformité visuelle

Les captures PNG/GIF de preuve vivent sous :

- [`docs/compliance/renders/`](docs/compliance/renders/) — jeux de tests waves 27–37 (compliance)
- [`docs/renders/`](docs/renders/) — sorties récentes (ex. wave 41 atmosphère, iso 36)

Ne pas dupliquer les assets sans besoin ; les README pointent vers ces dossiers.
