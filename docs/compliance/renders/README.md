# Renders compliance (Waves 27–37)

Captures PNG/GIF générées par les smokes et sessions de validation visuelle. **Ne pas supprimer** sans accord — servent de preuves de régression visuelle.

| Préfixe | Contenu |
|---------|---------|
| `wave27_*` | Rendu top-down chunks + macro + pipeline |
| `wave28_*` | Settlements |
| `wave29_*` | Réseau routier |
| `wave30_*` | Flux commerciaux |
| `wave31_*` | Diffusion culturelle |
| `wave32_*` | Polities |
| `wave36_*` / `wave37_*` | Iso voxel + timelapse |

Renders plus récents (ex. atmosphère wave 41) : [`../../renders/`](../../renders/).

Index documentation : [`../../README.md`](../../README.md).
